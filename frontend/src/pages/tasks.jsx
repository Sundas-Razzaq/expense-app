const TasksPage = () => {
    return (
        <main>
            <h1>Tasks</h1>
            <p>Protected placeholder page for backend and route testing.</p>
        </main>
    );
};

export default TasksPage;